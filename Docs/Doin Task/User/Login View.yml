info:
  name: Login View
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/userlogin/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
